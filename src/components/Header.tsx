function Header() {
  const titleHeader = "Mon budget";
  
  return (
    <header className="header">
      <h1 className="header__titre">{titleHeader}</h1>
    </header>
  );
}

export default Header;
